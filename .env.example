# Base URL of the awarome-BE API (no trailing slash), e.g. http://localhost:8083/api/v1
AWAROME_API_BASE_URL=

# Same x-awrm-api-key value awarome-BE expects on every request (API_KEY in awarome-BE's .env)
AWAROME_API_KEY=

